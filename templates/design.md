# Design: <title>

Change: `<change-id>`
Round: 1

## Background and goal

Why this change exists, in the user's own terms. What is broken or missing.

## Confirmed decisions

What the user has actually settled, and when. Only put things here that the
user said. An inference goes in `Proposed design`, not here.

## Open questions

Questions whose answer would change the direction of the work. While anything
is listed here the change stays `draft` and implementation does not start.

Write `none` when there are none. Do not answer them yourself and record the
answer here.

## Non-goals

What this change deliberately does not do, especially the nearby thing a
reader would otherwise assume is included.

## Current state with evidence

What is in the project today, with file paths and, where it matters, the lines
or commands you read it from. Not what you remember or expect.

## Proposed design and trade-offs

The approach, and what you rejected and why. If two readings of the request
both survive, that belongs in `Open questions` instead.

## Files in scope

Every file the implementer may touch. A file that is not listed is out of
scope; widening the list is a question, not a judgment call.

## Ordered tasks

1. …
2. …

Each one small enough to be checked off on its own.

## Acceptance criteria

| # | Action | Expected observable result | How to verify |
|---|---|---|---|
| AC1 | | | |
| AC2 | | | |

Every row is `action → observable result → how to verify`. Check each one
against what is actually in the project: a criterion that names a fixture row,
a flag, or a file that does not exist forces the implementer into an
unrecorded judgment call.

## Risks and approval boundaries

What could go wrong, and what the implementer must stop and ask about rather
than decide: irreversible deletion, migrations, writes to external systems,
publishing, production changes, and anything this project's own rules gate.

## Amendments

Empty on round 1.

When a review finds a criterion, task, or scope boundary is itself wrong and
the user settles it, record the change here rather than editing the section
above silently.

| Round | Date | What changed | The user's words |
|---|---|---|---|
| | | | |

Only what the user's answer settles. Nothing else in the design moves.
