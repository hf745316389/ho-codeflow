# 设计：<标题>

变更：`<change-id>`
轮次：1

> 字段名、状态值和文件名保持英文；正文用中文。

## Background and goal（背景与目标）

这次变更为什么存在，用用户自己的说法。哪里坏了或缺了什么。

## Confirmed decisions（已确认决定）

用户实际拍板的内容，以及时间。只写用户说过的。你的推断放到
`Proposed design`，不放这里。

## Open questions（待答问题）

答案会改变工作方向的问题。只要这里还列着东西，变更就停在 `draft`，不进入实现。

没有就写 `none`。不要自己回答后把答案写在这里。

## Non-goals（非目标）

这次变更刻意不做的事，尤其是读者会顺理成章以为包含在内的那件邻近的事。

## Current state with evidence（现状与证据）

项目今天的实际情况，附文件路径；必要时附你读到的行号或命令。不是你记得的，也不是
你预期的。

## Proposed design and trade-offs（方案与取舍）

采用的方案，以及你否决了什么、为什么。如果对请求的两种理解都站得住，那属于
`Open questions`，不属于这里。

## Files in scope（涉及文件）

实现者可以改动的每一个文件。没列出的文件就是范围外；扩大列表是一个问题，不是一次
自行判断。

## Ordered tasks（任务拆解）

1. …
2. …

每一项小到可以单独勾掉。

## Acceptance criteria（验收标准）

| # | Action | Expected observable result | How to verify |
|---|---|---|---|
| AC1 | | | |
| AC2 | | | |

每一行都是「操作 → 可观察结果 → 验证方式」。逐条对照项目里实际存在的东西核一遍：
一条引用了并不存在的 fixture 行、开关或文件的标准，会把实现者推进一次没人记录的
自行判断。

## Risks and approval boundaries（风险与审批边界）

可能出什么问题；以及实现者必须停下来问、而不是自己决定的事：不可逆删除、数据迁移、
写入外部系统、对外发布、生产环境变更，以及本项目自己的规则所管辖的任何操作。

## Amendments（修订记录）

第一轮为空。

当验收发现某条标准、任务或范围边界本身就是错的，并且用户拍了板，把这次改动记在这
里，而不是悄悄改上面的正文。

| Round | Date | 改了什么 | 用户的原话 |
|---|---|---|---|
| | | | |

只改用户的答复所决定的那部分。设计里的其他内容一律不动。
