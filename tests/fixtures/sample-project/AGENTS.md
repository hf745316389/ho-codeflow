# Project rules

- Python 3.10+, standard library only. Do not add third-party dependencies.
- Every new metric must have a unit test in `tests/`.
- Run `python -m unittest discover -s tests -q` before you report work as done.
- Keep public function signatures backward compatible.
